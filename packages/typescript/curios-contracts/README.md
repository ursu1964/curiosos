# @curiosos/curios-contracts

This package is the TypeScript package boundary for future representations of frozen Curios contract artifacts.

Canonical Curios semantics originate from frozen contract artifacts. TypeScript code in this package must mirror or consume those artifacts; it must not define canonical Curios semantics independently.

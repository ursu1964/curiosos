[project]
name = "curios-core"
version = "0.0.0"
description = "Curios-owned domain and application logic package boundary."
requires-python = ">=3.14,<3.15"
dependencies = [
  "curios-contracts",
]

[tool.uv.sources]
curios-contracts = { workspace = true }

[build-system]
requires = ["uv_build>=0.12.0,<0.13.0"]
build-backend = "uv_build"
